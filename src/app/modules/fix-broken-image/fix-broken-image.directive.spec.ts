import { FixBrokenImageDirective } from './fix-broken-image.directive';

describe('FixBrokenImageDirective', () => {
  it('should create an instance', () => {
    const directive = new FixBrokenImageDirective();
    expect(directive).toBeTruthy();
  });
});
